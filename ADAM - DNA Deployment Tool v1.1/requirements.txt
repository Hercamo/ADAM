# ADAM DNA Deployment Tool — pinned runtime dependencies
#
# Pinned to specific versions for reproducible builds. Update deliberately;
# CI should run `pip install -r requirements.txt` with no version drift.
# Versions match those used by ADAM DNA Tool v1.1/backend for cross-module
# consistency.

python-docx==1.1.2
pyyaml==6.0.2
click==8.1.7
